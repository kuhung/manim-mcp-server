# Core dependencies
mcp>=1.0.0
manim>=0.17.0

# Installation options:
# 1. Using uv (recommended, fast):
#    uv sync
# 
# 2. Using pip (traditional):
#    pip install -r requirements.txt -r requirements-dev.txt 